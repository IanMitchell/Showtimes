# == Schema Information
#
# Table name: aliases
#
#  id         :integer          not null, primary key
#  show_id    :integer
#  name       :string
#  created_at :datetime         not null
#  updated_at :datetime         not null
#

aoty:
  show: desch
  name: AOTY

fmp:
  show: fmp
  name: FMP
