<%= f.label field.attribute %>
<%= f.collection_select(
        field.attribute,
        f.object.class.public_send(field.attribute.to_s.pluralize),
        :first,
        :first,
        {},
        { multiple: false },
    )
%>
