<li>
  <%= fansub.show.name %> - <%= fansub.current_release.source.number %>
  <ul>
    <li>Aired: <%= fansub.current_release.source.air_date %></li>
    <%- fansub.current_release.staff.each do |staff| %>
      <%- if staff.finished? %>
        <li>✓ <%= staff.user.name %> - <%= staff.position.name %></li>
      <%- else %>
        <li><%= staff.user.name %> - <%= staff.position.name %></li>
      <%- end %>
    <%- end %>
  </ul>
</li>
